
name: 'LATAM API (Single)'
type: module
description: 'Single-module: per-country API config with configurable country list, sample endpoints, access control, internal OAuth Key Manager, optional rate limiting.'
package: 'Custom'
core_version_requirement: '^10 || ^11'
dependencies:
  - drupal:system
